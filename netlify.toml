[build]
  command = "npm install && npm run build -- --configuration production"
  publish = "dist/recipe-app/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

