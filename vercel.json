{
  "version": 2,
  "buildCommand": "npm install && npm run build -- --configuration production",
  "outputDirectory": "dist/recipe-app/browser",
  "installCommand": "npm install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}

